import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'OnlineDocumentValidator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
